<!DOCTYPE html>
<html>
  <head>
    <!-- Google tag (gtag.js) -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-2B0RKWHR4L"></script>
    <script>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());

      gtag('config', 'G-2B0RKWHR4L');
    </script>

    <title>SmokeShift</title>
    <%= favicon_link_tag '32.png', rel: 'icon', type: 'image/png' %>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
    <%= display_meta_tags(default_meta_tags) %>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  </head>

    <body class="bg-darkBackground min-h-screen flex flex-col">
    <!-- ヘッダー -->
    <% if current_user %>
      <%= render 'shared/before_login_header' %>
    <% else %>
      <%= render 'shared/header' %>
    <% end %>

    <div class="flex flex-grow">
      <!-- サイドバー -->
      <div class="w-32 fixed top-16 left-0 h-full">
        <% if current_user&.non_smoker? %>
          <%= render 'shared/non_smoker_sidebar' %>
        <% else %>
          <%= render 'shared/smoker_sidebar' %>
        <% end %>
      </div>
      <!-- メインコンテンツ -->
      <div class="flex-grow p-8">
        <%= render 'layouts/flash_messages' %>
        <%= yield %>
      </div>
    </div>
  </body>
</html>