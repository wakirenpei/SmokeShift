<div class="absolute top-20 left-0 right-0 ml-48 flex justify-start">
  <% flash.each do |message_type, message| %>
    <div class="<%= flash_background_color(message_type) %> max-w-md ml-4 px-4 py-2 text-gray-100 mb-4 rounded-lg">
      <%= message %>
    </div>
  <% end %>
</div>
